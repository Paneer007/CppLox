var res = 1;

fun main(){
    var adjMat = [];
    for(var i =0 ; i< 10;i = i+1){
        append(adjMat,1);
    }
    var mtx = mutex();
    pfor(var x: adjMat ){
        // lock(mtx);
        res = res + 1;
        // unlock(mtx);
    }    
    print res;
}

main();